#!/bin/sh
set -eu

. "$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)/common"
mkdir -p "$DATA_ROOT/data" "$DATA_ROOT/run" "$DATA_ROOT/log" "$DATA_ROOT/rdp-drives"
chmod 700 "$DATA_ROOT" "$DATA_ROOT/data" "$DATA_ROOT/run" "$DATA_ROOT/log" "$DATA_ROOT/rdp-drives"
write_config
