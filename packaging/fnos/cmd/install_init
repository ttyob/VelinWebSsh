#!/bin/sh
set -eu

# fnOS owns the package-variable root. Do not change its mode during the
# install-init phase; later callbacks create the remaining runtime paths.
mkdir -p "$TRIM_PKGVAR/data"
chmod 700 "$TRIM_PKGVAR/data"
